import React from 'react';

const LoadingSpinner = () => (
    <div className="spinner">
        {/* Add your loading spinner styles or SVG here */}
        Loading...
    </div>
);

export default LoadingSpinner;
